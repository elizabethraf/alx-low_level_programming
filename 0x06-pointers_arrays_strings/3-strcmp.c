#include "main.h"

/**
* _strcmp - Entry point
* @dest:compare two strings
* @src:compare two strings
* Print with _putchar
* Return: same if they are the same or else not the same
**/
int _strcmp(char *s1, char *s2)
{
	int k, l = strlen(s1), strlen(s2);
	int c, t =0;

	if (k == l)
	{
		for (c = 0; s2[t] !='\0'; c++)
		{
			if (s1[c] == s2[c])
					t++;
		}
		if (t ==c)

	return(0);
}

