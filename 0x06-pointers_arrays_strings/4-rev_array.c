#include "main.h"
/**
* reverse_array - Entry point
* @a:
* @n:
* Return: Always 0
**/
void reverse_array(int *a, int n)
{
	int temp = 0
		for (int i = 0; i < (length /2); i++)
		{
			temp = array[i];
			a[i] = array[length - i - 1];
			n[length - i - 1] = temp;
		}
}

