#include "main.h"
#include <stdio.h>

/**
* mul - Entry point
* @a: recieves two integers
* @b: recieves two integers
* Print with _putchar
* Return: 0 always if (succesful)
**/
int mul(int a, int b)
{
	return (a * b);
}
