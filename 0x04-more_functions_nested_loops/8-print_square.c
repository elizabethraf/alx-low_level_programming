/**
* print_square - Entry point
* Print with _putchar
*
* return if 0 print only new line
* Return: 0 always if (successful)
**/
void print_square(int size)
{
	int wid, hgt;

	 size < 0

	{
	if (hgt = 0; wid = 0; size++)
		for (wid = 0; wid < size; wid++)

		_putchar(#);
		{
		if (wid == 0 (hgt == size - 1)
			continue;
		}
			_putchar('\n');
		}
}
