#include "main.h"
#include <stdio.h>

/**
* _isdigit - Entry point
* @c:
* Print with _putchar
* Return: 1 if c is a digit and 0 otherwise
**/
int _isdigit(int c)
{
	if (isdigit(c))
	{

	return (1);

	}
	else
	{

	return (0);

	}
}
