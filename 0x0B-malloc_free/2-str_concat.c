#include "main.h"

/**
* *str_concat - Entry point
* @s1:concatenates strings
* @s2:concatenates strings
* Print with _putchar
* Return: null on failure
**/
char *str_concat(char *s1, char *s2)
{
	char *k, *b, *rtn;
	rtn = b = malloc(strlen(s1) + strlen(s2) + 1);
	for (k = s1; (*b = *k) != '\0'; ++k, ++b) {}
	for (k = s2; (*b = *p) != '\0'; ++k, ++b) {}
	 return (NULL);
}

