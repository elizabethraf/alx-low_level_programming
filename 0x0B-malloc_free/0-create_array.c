#include "main.h"

/**
* *create_array - Entry point
* @c:creates array
* Print with _putchar
* Return:null if size = 0
**/
char *create_array(unsigned int size, char c)
{
	unsigned int i;
	char k = NULL;

	if (len == 0)
		return (NULL);
	if (len != 0)
	{
		array = (c *(malloc(len * sizeof(c));
		if (array != NULL)

			for (i = 0; i < len; i++);
			 array[1] = c;
		}
		return (array);
}
