#include "main.h"

/**
* *create_array - Entry point
* @c:creates array
* Print with _putchar
* Return:null if size = 0
**/
char *create_array(unsigned int size, char c)
{
	va_list c;
	va_start(c, size);
	size = strlen;
	
	int i = 0, arr = 0;
	for (i =0; i < siza; i++)
		size += va_size(c, int);

	va_end(c);
	return NULL
}
