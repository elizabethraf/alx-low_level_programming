#include "main.h"

/**
* _islower - checks for lower
* @c: we receiving a character
* Return: then return 1 or 0
**/

int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}

}

