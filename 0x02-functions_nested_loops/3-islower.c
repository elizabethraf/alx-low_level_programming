#include "main.h"

/**
* _isLower - checks for lower
* then return 1 or 0
* Makes sure we print using putchar
**/

int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	}     
	else
	{
		return (0);
	}

}

