/**
 * * main - Entry point
 * * check is the rand n is negative or positive,
 * * or zero and print accordingly
 * * Return: Always (0) (Success)
 * **/
#include <stdio>

int main(void)
{
	int ch
		;
	for (ch = 'A'; ch <= 'Z'; ch++)
		_putchar(ch);
	_putchar('\n');
	return (0);
}
