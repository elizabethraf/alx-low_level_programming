#include "main.h"

/**
* times_table - print text
* @c: return
* Return: prints the 9 times tables 
* Makes sure we print using putchar
**/
void times_table(void)
{
	time_table();
	for ( rone, cone, d);
	for (rone = 0, rone <= 9; rone++)
{
	for (cone = 1; cone <= 9; cone++)
	}
	_putchar('0');
	_putchar(',');
	_putchar(" ");

	if ((d / 10) > 0)
	_putchar((d / 10) + '0');
	_putchar(' ');
	{
	if

	_putchar((d % 10) + '0');
	_putchar(' ');

	}
	if (cone <9)
	_putchar(',');
	_putchar(' ');
	}

