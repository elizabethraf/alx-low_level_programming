#include "main.h"

/**
* is_palindrome - Entry point
* @s:string is a palindrome
* Print with _putchar
* Return: always 0
**/
int is_palindrome(char *s)
{
	s = strlen;
	*s = i;

	 len = strlen(s);

	if ((i = 0) i < s; i++)

		if (s != 0[len - i - 1])
			return (0);
	is_palindrome(s + 1);

	return (1);
}

