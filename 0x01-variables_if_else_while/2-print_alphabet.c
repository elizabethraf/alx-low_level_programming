#include <stdio.h>
/**
* main - Entry point
* check is the rand n is negative or positive,
* or zero and print accordingly
* Return: Always (0) (Success)
**/
int main(void)
{

int ch;

for (ch = 'a'; ch <= 'z'; ch++)
putchar (ch);
putchar('\n');
return (0);

}
