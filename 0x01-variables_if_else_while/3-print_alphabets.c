/**
* main - Entry point
* check is the rand n is negative or positive,
* or zero and print accordingly
* Return: Always (0) (Success)
**/
#include<stdio.h>
int main(void)
{
	in ch;
	for(ch = 'a'; <= 'z'; ch++)
		putchar(ch);
	putchar('/n');

	for (ch= 'A'; ch<= 'Z'; ch++)
		putchar(ch);
	putchar ('\n');
	getch();
}
