/**
* main - Entry point
* check is the rand n is negative or positive,
* or zero and print accordingly
* Return: Always (0) (Success)
**/

#include <stdio.h>
 
int main (void) {

   int a = 10;

   while( a < 20 ) {
      printf("value of a: %d\n", a);
      a++;
      printf('\n');
   }
 
   return (0);
