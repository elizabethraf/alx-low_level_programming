/**
* main - Entry point
* check is the rand n is negative or positive,
* or zero and print accordingly
* Return: Always (0) (Success)
**/
#include<stdio.h>
int main(void)
{
	char ch;

    for (ch = 'a'; ch <= 'z'; ch++)
    {
		if (
        putchar(ch);
    }
    for (ch = 65; ch <= 90; ch++)
    {
        putchar(ch);
    }
    putchar(10);

    return (0);
}

