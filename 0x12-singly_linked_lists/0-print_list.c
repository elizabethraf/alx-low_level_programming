#include "lists.h"

/**
* print_list - check code
* @h: print elements
* Return: the number of nodes
*
**/
size_t print_list(const list_t *h)
{
	unsigned int count = 0;
	int str;

	while ( h != NULL)
	{
		if (h == NULL | str == NULL)
			return (NULL);
			printf("[0] (nil)");
	{
		printf ("[%d]", const *);
		printf ("[%s]", const *);
	}
	h = h;
	count++;
	printf("\n");
	}
	return (count);
}

